<%= form_with(model: ticket, local: true) do |form| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :source_city_id %>
    <%= form.text_field :source_city_id %>
  </div>

  <div class="field">
    <%= form.label :destination_city_id %>
    <%= form.text_field :destination_city_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :bus_company_id %>
    <%= form.text_field :bus_company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
