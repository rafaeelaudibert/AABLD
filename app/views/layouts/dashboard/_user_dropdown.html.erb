<li class="dropdown"><a href="#" data-toggle="dropdown" class="nav-link dropdown-toggle nav-link-lg nav-link-user">
    <img alt="image" src="<%= current_user.avatar_url(200) %>" class="rounded-circle mr-1">
    <div class="d-sm-none d-lg-inline-block">Hi, <%= current_user.first_name %></div></a>
    <div class="dropdown-menu dropdown-menu-right">
        <div class="dropdown-title">Entrou <%= ((Time.zone.now - current_user.last_sign_in_at) / 1.minute).round %> min atrás</div>
        <a href="features-profile.html" class="dropdown-item has-icon">
            <i class="fa fa-user"></i> Perfil
        </a>
        <a href="features-settings.html" class="dropdown-item has-icon">
            <i class="fa fa-cog"></i> Configurações
        </a>
        
        <div class="dropdown-divider"></div>
        <%= link_to(destroy_user_session_path, class: 'dropdown-item has-icon text-danger') do %>
            <i class="fas fa-sign-out-alt"></i> Sair
        <% end %>
    </div>
</li>